let item = '*';
for (i=0; i<7; i++) {
  console.log(item);
  item += '*';
}
  console.log('Second triangle');

// once more

for (let line = "#"; line.length < 8; line += "#") {
  console.log(line);
}
